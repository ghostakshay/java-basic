package com.ghost.finalTest;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Iterator;
import java.util.List;
/**
 * 
 * @author akshayp
 * Write Program to calculate elements in array if sum of all elements is even.
 * If not then keep removing element from 1st index.
 *
 */
public class NeoSoft {
	
/**
 * @param args
 */
public static void main(String[] args) {
	/*
	Integer [] A = {1,2,1,3,5,3};
	int count = 0;
	 List<Integer> list = Arrays.asList(A);
	 ArrayList<Integer> l = new ArrayList<Integer>(list);
	 int j =0;
	 int k =0;
	 Iterator<Integer> iterator = l.iterator();
		while (iterator.hasNext()) {
			Integer value = iterator.next();
			
			for(int i=0;i<l.size();i++) {
				count = count + l.get(i);
			}
			
			if (count % 2 == 0) {
				break;
			}
			iterator.remove();
			count = 0;

		}*/
	
	byte b =1;
	b+=1;
	System.out.println(b);
	
	
	
}

}
